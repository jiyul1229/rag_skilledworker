# rag_pipeline/.env.example
# 이 파일을 .env로 복사한 뒤 값을 채우세요. (.env는 .gitignore에 포함)
#
# Gemini API 키 (Google AI Studio 또는 GCP Vertex의 API 키)
# 코드는 다음 순서로 탐색: GCP_PROJECT_ID → GEMINI_API_KEY → GOOGLE_API_KEY
GCP_PROJECT_ID=

# (선택) 모델 오버라이드. 기본값: gemini-2.5-flash
# VERTEX_MODEL=gemini-2.5-pro
